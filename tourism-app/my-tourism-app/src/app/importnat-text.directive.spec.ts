import { ImportnatTextDirective } from './importnat-text.directive';

describe('ImportnatTextDirective', () => {
  it('should create an instance', () => {
    const directive = new ImportnatTextDirective();
    expect(directive).toBeTruthy();
  });
});
